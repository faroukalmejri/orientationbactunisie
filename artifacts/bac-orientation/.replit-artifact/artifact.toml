kind = "web"
previewPath = "/"
title = "BAC Orientation"
version = "1.0.0"
id = "artifacts/bac-orientation"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19778

[services.development]
run = "pnpm --filter @workspace/bac-orientation run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/bac-orientation", "run", "build" ]
publicDir = "artifacts/bac-orientation/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19778"
BASE_PATH = "/"
